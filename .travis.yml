sudo: false

language: python
python:
  - "3.6"
  - "3.7"
  
matrix:
  include:
    - python: "3.7"
      sudo: true
      dist: "xenial"

install:
  - travis_retry pip install tox-travis

script:
  - tox
